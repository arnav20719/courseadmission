import { prisma } from "@/lib/prisma"; export default async function AdminPage() { const leads = await prisma.lead.findMany({ orderBy: { createdAt: "desc" } }); return ( <div style={{ padding: "20px" }}> <h1 style={{ color: "black" }}>Admission Inquiries ({leads.length})</h1> {leads.length === 0 ? ( <p>No submissions yet. Submit a test form first.</p> ) : ( leads.map(lead => ( <div key={lead.id} style={{ border: "1px solid #ccc", padding: "15px", marginBottom: "10px", borderRadius: "8px", background: "white" }}> <p style={{ color: "black" }}><strong>Name:</strong> {lead.name}</p> <p style={{ color: "black" }}><strong>Phone:</strong> {lead.phone}</p> <p style={{ color: "black" }}><strong>Stream:</strong> {lead.stream || "N/A"}</p> <p style={{ color: "black" }}><strong>Message:</strong> {lead.message || "N/A"}</p> <p style={{ color: "black" }}><strong>Submitted:</strong> {new Date(lead.createdAt).toLocaleString()}</p> </div> )) )} </div> ); }
